'use client';

// This is a minimal page that serves as a route for Japanese language
// It will use the shared components with language context handling the translations
export { default } from '../page';
